{% extends 'base.html.twig' %}

{% block title %} Modifier la saison {% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-5 mb-5 text-uppercase">| MODIFIER LA SAISON |</h1>

        {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

        <button class="btn btn-dark"><a href="{{ path('season_index') }}" class="text-reset text-decoration-none">Revenir à la liste des saisons</a></button>

        {{ include('season/_delete_form.html.twig') }}
    </div>
{% endblock %}
