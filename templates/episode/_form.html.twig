{{ form_start(form) }}
    <div class="mb-3 text-center">NUMÉRO</div>
    <div class="mb-3 text-center">{{ form_widget(form.number) }}</div>
    <div class="mb-3 text-center">TITRE</div>
    <div class="mb-3 text-center">{{ form_widget(form.title) }}</div>
    <div class="mb-3 text-center">SYNOPSIS</div>
    <div class="mb-3 text-center">{{ form_widget(form.synopsis) }}</div>
    <div class="mb-3 text-center">SAISON ASSOCIÉE : {{ form_widget(form.season) }}</div>
    <div class="text-center"><button class="btn btn-dark mt-3 mb-3">{{ button_label|default('Enregistrer') }}</button></div>
{{ form_end(form) }}
